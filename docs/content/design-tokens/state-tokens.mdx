---
title: NCI Design System Color Tokens
---

import ColorChip from '../../src/components/ColorChip';
import { Button } from '@nciocpl/ncids-react';

# State color tokens

### USWDS state colors tokens provide flexible, accessible, customizable colors for warning, error, and other states

USWDS state color tokens are designed to be useful and effective for any project that has states or alerts. Each state token is drawn from a system color token and we only use token-based colors in official components.

## State color tokens

The state color palette is divided into five high-level role-based color families: `info`, `error`, `warning`, `success`, `emergency`, 
and `disabled`.

Each color family except `emergency` and `disabled` has five possible lightness grades, from `lighter` to `darker`, though 
not every family needs to include a color at each grade. Some grades may be set to `false` in your project's theme settings.

## State Colors 

 
| Color | Token | Default | Settings variable | Value |
| :--   | :-- | :-- | :-- | :-- |
|  <ColorChip color={"#f1c5d2"} />  | `'error-lighter'` 	| `'cranberry-20'` 	| `$theme-color-error-lighter` 		| `#f1c5d2` |
|  <ColorChip color={"#d66e8a"} />  | `'error-light'` 		| `'cranberry-40'` 	| `$theme-color-error-light` 			| `#d66e8a` |
|  <ColorChip color={"#bb0e3d"} />  | `'error'` 					| `'cranberry-60'` 	| `$theme-color-error` 						| `#bb0e3d` |
|  <ColorChip color={"#950b30"} />  | `'error-dark'` 			| `'cranberry-70'` 	| `$theme-color-error-dark` 			| `#950b30` |
|  <ColorChip color={"#700824"} />  | `'error-darker'` 		| `'cranberry-80'` 	| `$theme-color-error-darker` 		| `#700824` |
|  <ColorChip color={"#fdf2bf"} />  | `'warning-lighter'` | `'golden-5'` 		| `$theme-color-warning-lighter` 		| `#fdf2bf` |
|  <ColorChip color={"#fee685"} />  | `'warning-light'` 	| `'golden-10'` 	| `$theme-color-warning-light` 			| `#fee685` |
|  <ColorChip color={"#face00"} />  | `'warning'` 				| `'golden-20'` 	| `$theme-color-warning` 						| `#face00` |
|  <ColorChip color={"#ddaa01"} />  | `'warning-dark'` 		| `'golden-30'` 	| `$theme-color-warning-dark` 			| `#ddaa01` |
|  <ColorChip color={"#b38c00"} />  | `'warning-darker'` 	| `'golden-40'` 	| `$theme-color-warning-darker` 		| `#b38c00` |
|  <ColorChip color={"#dbf2f3"} />  | `'success-lighter'` | `'teal-5'` 		| `$theme-color-success-lighter` 		| `#dbf2f3` |
|  <ColorChip color={"#93d8dc"} />  | `'success-light'` 	| `'teal-20'` 	| `$theme-color-success-light` 			| `#93d8dc` |
|  <ColorChip color={"#3c989e"} />  | `'success'` 				| `'teal-40'` 	| `$theme-color-success` 						| `#3c989e` |
|  <ColorChip color={"#298085"} />  | `'success-dark'` 		| `'teal-50'` 	| `$theme-color-success-dark` 			| `#298085` |
|  <ColorChip color={"#1e4c4f"} />  | `'success-darker'` 	| `'teal-70'` 	| `$theme-color-success-darker` 		| `#1e4c4f` |
|  <ColorChip color={"#d4e7f2"} />  | `'info-lighter'` 		| `'cerulean-10'` | `$theme-color-info-lighter` 		| `#d4e7f2` |
|  <ColorChip color={"#66afd7"} />  | `'info-light'` 			| `'cerulean-30'` | `$theme-color-info-light` 			| `#66afd7` |
|  <ColorChip color={"#007bbd"} />  | `'info'` 						| `'cerulean-50'` | `$theme-color-info` 						| `#007bbd` |
|  <ColorChip color={"#006297"} />  | `'info-dark'` 			| `'cerulean-60'` | `$theme-color-info-dark` 				| `#006297` |
|  <ColorChip color={"#004971"} />  | `'info-darker'` 		| `'cerulean-70'` | `$theme-color-info-darker` 			| `#004971` |
|  <ColorChip color={"#e6e6e6"} />  | `'disabled-light'` 	| `'gray-10'` 	| `$theme-color-disabled-light` 			| `#e6e6e6` |
|  <ColorChip color={"#c9c9c9"} />  | `'disabled'` 				| `'gray-20'` 	| `$theme-color-disabled` 						| `#c9c9c9` |
|  <ColorChip color={"#adadad"} />  | `'disabled-dark'` 	| `'gray-30'` 	| `$theme-color-disabled-dark` 		 		| `#adadad` |
|  <ColorChip color={"#9c3d10"} />  | `'emergency'` 			| `'red-warm-60v'` 	| `$theme-color-emergency` 				| `#9c3d10` |
|  <ColorChip color={"#332d29"} />  | `'emergency-dark'`	| `'red-warm-80'` 	| `$theme-color-emergency-dark` 	| `#332d29` |
<br/>
<br/>