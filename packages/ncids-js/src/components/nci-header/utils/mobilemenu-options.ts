/**
 * NCIMegaMenu Options for display and init
 * @param {boolean} useUrlForNavigationId Use URL of link to pull navigation.
 */
export type MobileMenuOptions = {
	useUrlForNavigationId: boolean;
};
